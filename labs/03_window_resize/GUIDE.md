# Lab 3 — Window Resize Tracker (15 min)

## Task
Track `window.innerWidth` and show it.

### Requirements
- useState to hold width
- useEffect to add/remove resize listener
- Cleanup on unmount

### Hints
- Extract a custom hook `useWindowWidth()`

### Acceptance
- Width updates on resize
- No memory leaks
